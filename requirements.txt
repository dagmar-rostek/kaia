# KAIA – Kinetic AI Agent
# Cloud-Dependencies (Railway) — schlank, kein PyTorch
# Für lokale Entwicklung: pip install -r requirements-local.txt

# LLM Provider
anthropic>=0.40.0
mistralai>=1.0.0

# UI
streamlit>=1.40.0

# Utilities
python-dotenv>=1.0.0
requests>=2.32.0

# Memory & Datenbank
# ChromaDB ohne sentence-transformers: nutzt eingebaute ONNX-Embeddings (kein torch)
chromadb>=0.5.0
